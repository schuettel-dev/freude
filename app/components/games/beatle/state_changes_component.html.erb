<% if game.transition_allowed?(to_state: :guessing) %>
  <%= button_to t("game.beatle.proceed_to_guessing_phase"), game_state_path(game), method: :put, class: "attractive-button", "data-turbo-frame": "_top", params: { state: :guessing } %>
<% end %>
