<% content_for :navigation do %>
  <%= render Links::BackToComponent.new(game_path(@game), t("games.shared.back_to_game")) %>
<% end %>

<div class="px-8 flex flex-col gap-y-8">
  <div class="fle flex-col gap-y-2">
    <h1 class="text-5xl mb-8"><%= t("games.shared.final_ranking") %></h1>
  </div>

  <%= render Games::FinalRankingComponent.new(game: @game, highlight_player: @player, all: params[:all].present? ) %>
</div>
