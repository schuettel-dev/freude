<% content_for :navigation do %>
  <%= render Links::BackToComponent.new(game_path(@player.game), t("shared.actions.back_to_game")) %>
<% end %>

<div class="px-8 flex flex-col gap-y-8">
  <h1 class="text-5xl mb-8"><%= Player.model_name.human(count: 2) %></h1>

  <%= render Games::PlayersSectionComponent.new(player: @player) %>
</div>
