<section>
  <%= render CardComponent.new do %>
    <div class="flex flex-col gap-y-8">
      <h2 class="text-3xl font-bold uppercase text-indigo-700"><%= game_template.name %></h2>
      <p class="text-xl tracking-wide"><%= game_template.description %></p>
      <%= link_to t(".start_this_game"), new_game_template_instance_path(game_template), class: "button" %>
    </div>
  <% end %>
</section>
