<% if Current.user.present? %>
  <header>
    <div class="flex justify-between bg-gradient-to-br from-indigo-600 to bg-indigo-700 px-8 py-4 text-white text-lg">
      <%= content_for :navigation %>
    </div>
  </header>
<% end %>
